  {{articles}}
      <button class="btn btn-primary addNote" data-id="{{_id}}" data-toggle="modal" data-target="#noteModal">Add a Note</button>
      <a href="/savedArticles">
      <button class="btn btn-primary delArt" data-id="{{_id}}">Delete Article</button>
      </a>
      <br>